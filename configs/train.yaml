# name: 'cfm'
name: 'transformer'
# name : 'refiner'

learning_rate: 1.0e-3
batch_size_train: 64
batch_size_val: 64
num_epochs: 100
num_workers: 0

nray: 0
seed: 12345

# flow_match:
#   sigma: 0

sampler:
  solver: 'dopri5'
  steps: 25